<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interesting Tech</title>
    <link rel="icon" href="/images/Favicon-Irfansusanto20.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Halant:wght@600&display=swap" rel="stylesheet">
</head>

<body>
    <header>
        <nav class="navbar bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">
                    <img src="/images/Favicon-Irfansusanto20.png" alt="Logo" width="30" height="24"
                        class="d-inline-block align-text-top">
                    IT Blog
                </a>
                <div class="ml-auto">
                    <!-- Conditionally render login or logout links -->
                    {{!-- {{#if logged_in}} --}}
                    <a href="/">Home</a> |
                    <a href="/dashboard">Dashboard</a> |
                    {{!-- <button class="out-button" id="logout">Log out</button>
                    {{else}} --}}
                    <a href="/login">Log in</a>
                    {{!-- {{/if}} --}}
                </div>
            </div>
        </nav>
    </header>

    <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>




    <!-- Render script for logged in users only - Allows the Log Out button to function -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{else logging_in}}
    <script src="/js/password.js"></script>
    {{else sign_up}}
    <script src="/js/password.js"></script>
    {{/if}}

</body>

</html>